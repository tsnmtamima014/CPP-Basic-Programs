#include <iostream>
#include <map>
int main() {
    int n, x;
    std::map<int, int> f;
    std::cin >> n;
    while(n--) { std::cin >> x; f[x]+
        +;
        }
        for(auto const& [v, c] : f)
            std::cout << v << " : " << c << " ";
        return 0;
}

